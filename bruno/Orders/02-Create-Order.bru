meta {
  name: Create Order
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "dishes": [
      { "id": 1, "quantity": 2 },
      { "id": 2, "quantity": 1 }
    ]
  }
}
