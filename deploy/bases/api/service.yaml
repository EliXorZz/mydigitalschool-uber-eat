apiVersion: v1
kind: Service
metadata:
  name: uber-eat-api
spec:
  selector:
    app: uber-eat-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
