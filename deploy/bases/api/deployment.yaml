apiVersion: apps/v1
kind: Deployment
metadata:
  name: uber-eat-api
  labels:
    app: uber-eat-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uber-eat-api
  template:
    metadata:
      name: uber-eat-api
      labels:
        app: uber-eat-api
    spec:
      imagePullSecrets:
        - name: registry-secret
      containers:
        - name: fpm
          image: uber_eat_api_image
          envFrom:
            - secretRef:
                name: uber-eat-api-secret
          volumeMounts:
            - name: storage
              mountPath: /app/storage/app/public
          lifecycle:
            postStart:
              exec:
                command: [ 'sh', '-c', 'cp -r /app/. /var/www && ln -sfn /mnt/storage /var/www/storage/app/public' ]
        - name: nginx
          image: nginx:1.27
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: uber-eat-api-secret
          volumeMounts:
            - name: storage
              mountPath: /mnt/storage
              readOnly: true
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
              readOnly: true
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: uber-eat-api-storage
        - name: nginx-conf
          configMap:
            name: uber-eat-api-nginx-conf
