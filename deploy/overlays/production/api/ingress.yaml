apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uber-eat-api
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: traefik-force-https@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.uber-eat.mydigitalschool.server.dylanbattig.fr
      secretName: uber-eat-api-tls
  rules:
    - host: api.uber-eat.mydigitalschool.server.dylanbattig.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uber-eat-api
                port:
                  name: http
